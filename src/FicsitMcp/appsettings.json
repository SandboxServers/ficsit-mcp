{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },

  "//": "Surface config below. Every surface is OPTIONAL: omit a section (or leave its URL blank) and that surface stays dormant without affecting the others. Secrets (AdminToken, SharedSecret) are intentionally NOT set here — supply them via FICSITMCP_-prefixed env vars in your MCP client config so they never sit in a committed file. Env vars override anything in this file.",

  "DedicatedServer": {
    "BaseUrl": "",
    "DangerousAcceptAnyCert": false
  },

  "Frm": {
    "BaseUrl": "",
    "TransportMode": "Direct"
  },

  "FinBridge": {
    "ListenUrl": ""
  }
}
